[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
# E501 line-length is handled by ruff format
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
# Test files can use assert statements freely
"test_*.py" = ["S101"]
